//Inicialmente, declaramos as variáveis:

var indice = 13
var k = 0
soma = 0

// Em seguida, vamos declarar o comando while, que vai executar uma rotina contínua verdadeira, 
//até que a condição seja falsa e o programa encerre:

while (k<indice) {
    console.log(soma)
    k=k+1; soma = soma+k

}